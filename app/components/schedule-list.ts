import { Observable } from '@nativescript/core';

export class ScheduleListComponent extends Observable {
    constructor() {
        super();
    }

    onSelect() {
        // Handle schedule selection
    }
}